"use strict";
// Array types, Tupple
const names = ['fahim', 'omer', 'maruf'];
const rolls = [20, 30, 1, 3];
// union array types
const values = [20, 's', false];
// tupple types
const tupple = ['hridoy', 20, false];
// providing | show all method of number | string etc
// names.map(name => name.)
// rolls.map(roll => roll.)
