// Array types, Tupple

const names: string[] = ['fahim', 'omer', 'maruf'];
const rolls: number[] = [20,30,1,3]

// union array types
const values:(string|number|boolean)[] = [20, 's', false]

// tupple types
const tupple:[string, Number, boolean] = ['hridoy', 20, false]

// providing | show all method of number | string etc
// names.map(name => name.)
// rolls.map(roll => roll.)